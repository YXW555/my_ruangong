<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.second.hand.trading.server.dao.MembershipOrderDao">
    <resultMap id="BaseResultMap" type="com.second.hand.trading.server.entity.MembershipOrderModel">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="user_id" jdbcType="BIGINT" property="userId"/>
        <result column="membership_type" jdbcType="TINYINT" property="membershipType"/>
        <result column="order_number" jdbcType="VARCHAR" property="orderNumber"/>
        <result column="amount" jdbcType="DECIMAL" property="amount"/>
        <result column="payment_status" jdbcType="TINYINT" property="paymentStatus"/>
        <result column="payment_way" jdbcType="VARCHAR" property="paymentWay"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="payment_time" jdbcType="TIMESTAMP" property="paymentTime"/>
        <result column="duration_months" jdbcType="INTEGER" property="durationMonths"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, membership_type, order_number, amount, payment_status, payment_way,
        create_time, payment_time, duration_months
    </sql>

    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="com.second.hand.trading.server.entity.MembershipOrderModel"
            useGeneratedKeys="true">
        insert into sh_membership_order (user_id, membership_type, order_number, amount,
                                         payment_status, payment_way, create_time, payment_time, duration_months)
        values (#{userId,jdbcType=BIGINT}, #{membershipType,jdbcType=TINYINT}, #{orderNumber,jdbcType=VARCHAR},
                #{amount,jdbcType=DECIMAL}, #{paymentStatus,jdbcType=TINYINT}, #{paymentWay,jdbcType=VARCHAR},
                #{createTime,jdbcType=TIMESTAMP}, #{paymentTime,jdbcType=TIMESTAMP}, #{durationMonths,jdbcType=INTEGER})
    </insert>

    <select id="selectByOrderNumber" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sh_membership_order
        where order_number = #{orderNumber,jdbcType=VARCHAR}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sh_membership_order
        where user_id = #{userId,jdbcType=BIGINT}
        order by create_time desc
    </select>

    <select id="selectByPaymentStatus" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sh_membership_order
        where payment_status = #{paymentStatus,jdbcType=TINYINT}
        order by create_time desc
    </select>

    <update id="updateByPrimaryKeySelective" parameterType="com.second.hand.trading.server.entity.MembershipOrderModel">
        update sh_membership_order
        <set>
            <if test="paymentStatus != null">
                payment_status = #{paymentStatus,jdbcType=TINYINT},
            </if>
            <if test="paymentWay != null">
                payment_way = #{paymentWay,jdbcType=VARCHAR},
            </if>
            <if test="paymentTime != null">
                payment_time = #{paymentTime,jdbcType=TIMESTAMP},
            </if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>

    <select id="sumAmountByPaymentStatus" resultType="java.math.BigDecimal">
        select COALESCE(SUM(amount), 0) from sh_membership_order
        where payment_status = #{paymentStatus,jdbcType=TINYINT}
    </select>

    <select id="sumAmountByDateRange" resultType="java.math.BigDecimal">
        select COALESCE(SUM(amount), 0) from sh_membership_order
        where payment_status = #{paymentStatus,jdbcType=TINYINT}
        and payment_time >= #{startDate,jdbcType=TIMESTAMP}
        and payment_time &lt; #{endDate,jdbcType=TIMESTAMP}
    </select>
</mapper>

